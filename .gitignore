# OS / editor cruft
*:Zone.Identifier
.DS_Store

# Node (no runtime deps, but keep stray installs out of the tree)
node_modules/
npm-debug.log*

# Local Claude Code config (per-machine, not shared)
CLAUDE.md
.claude/settings.local.json
.claude/launch.json
.claude_settings.json

# Auto Claude tooling artifacts
.auto-claude/
.auto-claude-status
.auto-claude-security.json
